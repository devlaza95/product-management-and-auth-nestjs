export enum SortOrderEnum {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum SortEnum {
  ID = 'id',
  NAME = 'name',
  PRICE = 'price',
  QUANTITY = 'quantity',
}

export enum SignEnum {
  GREATER_THAN = 'Greater than',
  LESS_THAN = 'Less than',
}
